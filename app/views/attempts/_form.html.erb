<%= simple_form_for [cocktail, attempt] do |f| %>
  <%= f.input :title %>
  <%= f.input :comment %>
  <%= f.input :date_of_attempt %>
  <%= f.submit class: 'btn-flat'%>
  <%= f.submit 'Back', class: 'btn btn-ghost'%>
<% end %>
