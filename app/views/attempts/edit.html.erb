<div class="form">
  <h5 style="text-align: center">Edit attempt</h5>
  <%= simple_form_for [@cocktail, @attempt] do |f| %>
  <%= f.input :title %>
  <%= f.input :comment %>
  <%= f.input :photo, as: :file %>
  <%= f.submit class: 'btn btn-primary'%>
<% end %>
</div>
